# Keep Gradle from auto-selecting broken local Java installations.
# The run scripts launch Gradle with JAVA_HOME set from the javac found on PATH; this also helps users who run Gradle manually.
org.gradle.daemon=false
org.gradle.java.installations.auto-detect=false
org.gradle.java.installations.fromEnv=JAVA_HOME
